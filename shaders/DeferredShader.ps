uniform sampler2D tex;

varying vec3  normal;
varying vec3 vertexPos;
varying vec2 texCoord;
void main()
{
  gl_FragData[0] = vec4(normal,1.0);
  gl_FragData[1] = vec4(vertexPos,1.0);
  gl_FragData[2] = texture2D( tex, texCoord ) *0.00001 + vec4(1.0);
}
